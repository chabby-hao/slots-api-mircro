syntax = "proto3";

package gitlab.haloapps.com/batatagames/slots/backend/slots-api-micro/user/rpc;

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service Rpc {
  rpc Ping(Request) returns(Response);
}
